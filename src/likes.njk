---
title: Likes
description: Some cool things I found on the internet.
layout: base.njk
permalink: /likes/index.html
eleventyExcludeFromCollections: true
---
<section class="container h-feed text-center post">
  <h1 class="p-name tight">
    {{ title }}
  </h1>
  <p>Some cool things I found on the internet.</p>
  <div class="hr shadow mt1 mb2"></div>
  <ul class="list-reset grid grid3 gap1 mt2">
    {% for item in collections.likes | reverse %}
      <li class="d-block">
        <a class="d-block w100 u-url p-name like-link shadow-md-hover hover-scale-sm no-underline px2 py1 mx-auto" href="{{ item.url }}" style="border: 1px solid var(--border);">
          <span class="d-block" style="margin-bottom: .5rem;">{{ item.data.title }}</span>
          <time class="d-block regular small">{{ item.data.published | date('MMM DD, YYYY') }}</time>
        </a>
      </li>
    {% endfor %}
  </ul>
</section>
