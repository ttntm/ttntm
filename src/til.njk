---
layout: page.njk
title: Today I Learned...
permalink: /til/index.html
---
{%- for item in collections.til | reverse -%}
  <article>
    <h2 class="h5">
      <span>
        <a id="{{ item.data.weight }}" href="#{{ item.data.weight }}" class="no-underline">#{{ item.data.weight }}</a>
      </span>
      &nbsp;
      {{ item.data.title }}
    </h2>
    <p class="small text-sec">Posted: {{ item.date | date('MMMM DD, YYYY') }}</p>
    {{ item.templateContent | safe }}
  </article>
{%- endfor -%}